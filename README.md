# portfolio

This is a showcase of my coding projects through Codex.

## Setup

Install the project requirements before running any code:

```bash
pip install -r requirements.txt
```

Story generation and the online transcription features also require the
`OPENAI_API_KEY` environment variable to be set.
